const logger = require("./utils/Logger");
/**
 * Centralized constants for the application
 */

// Shopify API version to use throughout the application
const SHOPIFY_API_VERSION = '2025-04';

module.exports = {
  SHOPIFY_API_VERSION
};
